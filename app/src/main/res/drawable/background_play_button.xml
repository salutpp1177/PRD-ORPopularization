<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- fill-in color -->
    <solid android:color="@color/colorWhite" />

    <!-- border width and color -->
    <stroke
        android:width="3dp"
        android:color="@color/welcome_string"
        />

    <!-- android:radius -->
    <corners
        android:radius="75dp"
        />
</shape>
